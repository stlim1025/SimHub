{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SessionStarted",
  "type": "object",
  "required": ["sessionRef", "trackId", "sessionType"],
  "properties": {
    "sessionRef": {
      "type": "string",
      "description": "에이전트가 로컬에서 인식하는 게임 세션 고유 식별자 (예: F1 sessionUID의 Hex/String 표현)"
    },
    "trackId": {
      "type": "integer",
      "description": "게임 UDP에서 제공하는 원시(Raw) 트랙 ID 정수값"
    },
    "sessionType": {
      "type": "string",
      "description": "게임 내 세션 모드 (예: TimeTrial, Practice, Qualifying, Race)"
    }
  }
}
